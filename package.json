{
  "name": "autopilot-test-framework",
  "version": "1.0.0",
  "description": "Autopilot based Flutter automation framework",
  "scripts": {
    "test": "mocha tests/specs/**/*.spec.js --no-parallel --jobs 1"
  },
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.13.2",
    "chai": "^6.2.2",
    "expect": "^30.2.0",
    "mocha": "^11.7.5"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.2.0"
  }
}